name: healthcare_dwh
version: "1.0.0"
config-version: 2

profile: healthcare_dwh

model-paths:   ["models"]
test-paths:    ["tests"]
seed-paths:    ["seeds"]
macro-paths:   ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  healthcare_dwh:
    +materialized: table
    +dataset: healthcare_dwh
    staging:
      +materialized: view
